#!/bin/sh /etc/rc.common
# Init script for Shairport-Sync AirPlay 2

START=95
STOP=10

USE_PROCD=1

start_service() {
    procd_open_instance
    procd_set_param command /usr/bin/shairport-sync -c /etc/config/shairport-sync
    procd_set_param respawn
    procd_close_instance
}
